// @ts-check
 
/** @type {import("next").NextConfig} */
const nextConfig = {
	/* config options here */
    images: {
        domains: [
			"images.unsplash.com",
			"assets.aceternity.com"
		]
    }
}
   
module.exports = nextConfig